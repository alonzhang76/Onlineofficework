## plm-work 对齐 Onlineofficework：CloudBase 迁移 + 登录 + 云存储（2026-09-13）
- **存储迁移**：utils/supabase.js 删除，新增 utils/cloudbase.js（CloudBase HTTP API：`{env}.api.tcloudbasegateway.com`，鉴权 `/auth/v1/token` grant_type=password/refresh_token（refresh 用后轮换），数据 PostgREST 兼容 `/v1/rdb/rest/app_data_store`，PG 行形态 `{id, data:{store_key,payload,updated_at}}`，id=store_key）。5 个 db 文件 require 已切换，导出接口不变（pullAll/pull/push/flushQueue/takeCloud/setSuppressPush）。
- **统一登录**：新增 pages/login/login（邮箱+密码，用户会话独立于同步共享账号 sync@lori.app），读 `user_app_permissions`（id='perm::<小写邮箱>'，data.perms，网页版 app id 映射：wicketorders→trade、orderschedule→schedule、incomeexpense→income），home 按权限过滤应用，管理员 alonzhang76@outlook.com 全可见。app.json 入口页改为 login。home 有用户徽章+退出。
- **云存储文件中心**：utils/cb-files.js + pages/cloudfiles/cloudfiles（?app=wage|trade|schedule，根目录 wage/ wicketorders/ orderschedule/）。列目录走云函数 tcb-file-list（已升级返回 bucket+cloudObjectId，需重新部署云函数）；上传 get-objects-upload-info + COS PUT（wx.request ArrayBuffer）；下载 get-objects-download-info；删除 delete-objects。三应用 more/hub 已加入口。
- **冒烟+端到端验证**（真实环境）：登录 200、upsert 201、in 查询、单键读、takeCloud、删除全通过；云端确认 wicketorders__orderRecords 等 171 个键存在；**wage_records 云端无数据**（网页版工资数据未上云，小程序首推会创建）。
- **遗留**：① user_app_permissions 为空表，普通用户需网页版门户管理面板配置权限后才能看到应用；② 云函数 tcb-file-list 改过需重新部署；③ 微信后台需把 https://onlineofficework-d4e93l98bdf879e.api.tcloudbasegateway.com 加入 request 合法域名；④ 服装外贸(saintysys)/不锈钢贸易(stainlessbusiness) 属独立应用不纳入 plm-work。
## 服装外贸 + 不锈钢贸易两个独立小程序制作完成（2026-09-13 晚）
- **saintysys-mp**（服装外贸，云端裸键）：12 个模块——订单/生产/面料/辅料/水洗/样衣/出货/收汇/发票/付款/通讯录/快递物流。
- **stainless-mp**（不锈钢贸易，stainlessbusiness__ 前缀）：7 个模块——采购订单/销售订单/询价单/采购收退货/销售退货/通讯录/备忘录。
- 架构：`_app-template/` 通用模板 + `build-apps.js` 构建脚本生成两项目；utils/list-page.js 是 schema 驱动的通用列表页工厂（搜索/状态筛选/增删改/自动计算 金额=数量×单价）；utils/db.js 单应用数据层（LWW+推送队列）；登录=CloudBase 账号（独立应用不做权限过滤）。
- 字段来源：先抽样云端真实数据（shippings/payments/consumptions/pl_records/samples/express 有真实字段），空键从网页版源码提取建对象处字段。
- 验证：两项目 check-project 全过；test-new-apps.js 16/16（loadAll/save/入队/takeCloud/离线安全/autoSum）；真实云端冒烟 push(201)/pullAll 白名单/错误密码拒绝，测试键已全部清理（0 残留）。
- 待用户：注册两个新 appid 填入 project.config.json；加 request 合法域名；修改业务字段直接编辑 pages/<module>/schema.js 后重跑 node build-apps.js。

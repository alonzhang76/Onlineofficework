.pur-hero {
  background: linear-gradient(135deg, #7F77DD 0%, #5856D6 55%, #3B39A8 100%);
  border-radius: 28rpx; padding: 44rpx 36rpx; margin-bottom: 24rpx;
}
.pur-hero-title { font-size: 40rpx; font-weight: 700; color: #fff; }
.pur-hero-sub { font-size: 22rpx; color: rgba(255,255,255,0.85); letter-spacing: 2rpx; margin-top: 6rpx; }
.mod-grid { display: flex; flex-wrap: wrap; }
.mod-item { width: 33.33%; display: flex; flex-direction: column; align-items: center; padding: 20rpx 0; }
.mod-icon {
  width: 88rpx; height: 88rpx; border-radius: 24rpx;
  display: flex; align-items: center; justify-content: center; margin-bottom: 10rpx;
  box-shadow: 0 6rpx 14rpx rgba(0,0,0,0.12), inset 0 2rpx 3rpx rgba(255,255,255,0.4);
}
.mod-icon:active { transform: scale(0.92); }
.mod-icon-text { font-size: 44rpx; line-height: 1; }
.mod-name { font-size: 24rpx; color: #1C1C1E; }
